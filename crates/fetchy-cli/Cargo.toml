[package]
name = "fetchy-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish = false
description = "Command-line harness for the Inspector Fetchy search engine."

[[bin]]
name = "fetchy-cli"
path = "src/main.rs"

[dependencies]
fetchy-core = { path = "../fetchy-core", features = ["all-formats"] }
